-- ============================================================
-- Уведомления администратора (2026-08-24)
--
-- last_seen_moderation_at — когда админ последний раз открывал
-- вкладку «Модерация». Бейдж колокольчика для админа считает
-- события status='moderation', обновлённые после этой отметки.
-- ============================================================

alter table public.profiles
  add column if not exists last_seen_moderation_at timestamptz;
