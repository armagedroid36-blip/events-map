// Динамический z-index для окон: каждое новое окно открывается ПОВЕРХ старых.
let z = 1990;

/** Следующий уровень: вызвать один раз при открытии окна (useRef). */
export function nextZ(): number {
  z += 10;
  return z;
}
